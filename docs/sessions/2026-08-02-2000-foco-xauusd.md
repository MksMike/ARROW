# Sessão 2026-08-02-2000 — foco único em XAUUSDm

| Campo | Valor |
|---|---|
| Máquina | PC-Home |
| Branch | `session/2026-08-02-foco-xauusd` |
| Commits | `ea8649d..<encerramento>` |
| Duração | ~10 min |

## Objetivo declarado

Registrar a decisão do chat: o projeto mede **um único instrumento**, `XAUUSDm` na conta Standard,
até existir catálogo de sensores e indicadores validados. Outros ativos, contas e corretoras
entram numa bateria posterior.

## Feito

| Arquivo | O que mudou | Por quê |
|---|---|---|
| `docs/decisions/0007-*.md` | criado | Decisão estrutural exige ADR (§16 regra 1) |
| `CLAUDE.md` §14 | escopo explícito por instrumento, conta e corretora | Era vago: dizia "outros instrumentos" sem citar conta ou corretora |
| `CLAUDE.md` §13, §18 | `XAUUSDz` removido | Não será auditado |
| `MQL5/Scripts/ARROW/DataAudit.mq5` | `InpSymbolAlt` vazio por padrão | Um símbolo por vez |
| `data/broker/BTCUSDm-*.csv` | movido para `data/_fora-de-escopo/` | ADR 0005 define `broker/` como ticks do XAUUSDm |
| `STATE.md` | pendências resolvidas | Estado real |

## Verificado

- [x] **`DataAudit.mq5` recompilou limpo:** `Result: 0 errors, 0 warnings, 465 ms elapsed`
- [x] **`data/broker/` contém apenas XAUUSDm** — `XAUUSDm-20260731.csv` e `_offset_log.csv`.
      O arquivo de BTCUSDm saiu de lá sem ser apagado.
- [x] `data/_fora-de-escopo/` tem `LEIA-ME.txt` explicando a origem do arquivo, para que ele não
      vire mistério em seis meses.

### Números que NÃO saíram de execução

Nenhum. Sessão de escopo e documentação; nenhuma medição nova.

## A justificativa que sustenta a decisão

O motivo imediato é foco, mas o que sustenta o ADR é estatístico e vale registrar em separado:

A §7 limita a 3 iterações de parâmetro por sensor porque testar muitas variações garante que
alguma passe por acaso. **Testar o mesmo sensor em N instrumentos é a mesma multiplicação, com uma
dimensão a mais e sem nenhum limite escrito.** Um sensor que falha em `XAUUSDm` e passa em
`XAUUSDz` não descobriu nada sobre o mercado — descobriu que existiam dois sorteios em vez de um.
E como as duas séries são praticamente o mesmo ativo, o teste extra não traz informação
independente: só chances extras de aprovar ruído.

A ordem correta inverte isso. Validar em um instrumento com o rigor todo e **depois** perguntar se
transporta. Aí a transferência é evidência a favor do sensor, porque foi prevista antes de medida.
Feita ao contrário, é seleção disfarçada de robustez.

## Não feito, e por quê

- **Coleta de tick de `XAUUSDz` não foi iniciada**, e essa é a única perda irreversível da
  decisão: a janela de retenção do broker rola, então o histórico não coletado hoje some. Aceito
  explicitamente no ADR — quando a bateria posterior acontecer, a coleta começa naquele momento.
- **A spec de `XAUUSDz` não foi registrada.** Não é perecível como o tick; pode ser lida a
  qualquer momento.
- **Nenhuma medição nova.** O mercado continua fechado e o `BrokerTickLogger` continua armado
  aguardando 22:05 UTC.

## Decisões tomadas dentro da sessão

| Decisão | Alternativa rejeitada | Vira ADR? |
|---|---|---|
| Um instrumento até haver catálogo validado | Auditar `XAUUSDz` em paralelo | **ADR 0007** |
| Arquivo do BTCUSDm movido, não apagado | Apagar; ou deixar em `broker/` | não — é higiene, e a ADR 0007 registra |
| A bateria posterior exigirá ADR próprio com correção para testes múltiplos declarada antes de medir | Abrir a bateria quando parecer oportuno | ADR 0007 |

## Perguntas para o chat

As três de fundo seguem abertas e sem avanço — **T do Gate 1**, **`confidence`**, **tese
mecânica**. Duas delas bloqueiam o primeiro sensor, que é o que a ADR 0007 acabou de tornar
pré-requisito de tudo mais.

Segue aberta também a de escopo levantada na sessão anterior: **os 12 anos de M1 do broker mudam o
desenho de medição do gap de fonte da §11.2?**

## Estado da árvore

- [x] `git status` limpo
- [x] Branch mergeada em `main`
- [x] `STATE.md` atualizado e sessão fechada
- [x] Push feito
